{
  "type": "bioactivity-filter",
  "label": "Bioactivity Filter",
  "category": "analysis",
  "icon": "activity",
  "color": "green",
  "description": "Filter compound bioactivity (pEC50 / pIC50) across multiple biological targets using ML-based models.",
  "defaultInputs": [
    {
      "id": "compoundList",
      "name": "Compound List",
      "type": "array",
      "required": true,
      "tooltip": "Input table of compounds (IDs and SMILES) to score."
    },
    {
      "id": "targetList",
      "name": "Target List",
      "type": "array",
      "required": true,
      "tooltip": "Targets or assays (EC50 / IC50) that bioactivity should be predicted against."
    }
  ],
  "defaultOutputs": [
    {
      "id": "bioactivityResults",
      "name": "Bioactivity Prediction Results",
      "type": "array",
      "tooltip": "Predicted pEC50 / pIC50 values and filters for each compound–target pair."
    }
  ],
  "configSchema": [
    {
      "key": "modelType",
      "type": "select",
      "label": "Available Tools",
      "options": [
        {
          "value": "dtign",
          "label": "DTIGN - Best for fast predictions"
        },
        {
          "value": "ligospace",
          "label": "Ligospace - Best for accurate predictions"
        }
      ],
      "defaultValue": "Ligospace",
      "tooltip": "Select the bioactivity engine: **DTIGN** for speed, **Ligospace** for higher accuracy."
    },
    {
      "key": "compoundfiles",
      "type": "file",
      "label": "Compound Files",
      "multiple": true,
      "tooltip": "Optional upload of structure files (e.g. SDF/CSV) to complement the compound input port."
    },
     {
      "key": "pvalue",
      "type": "number",
      "label": "pValue cutoff",
      "placeholder": "e.g. 5",
      "defaultValue": 5,
      "tooltip": "Minimum pEC50 / pIC50 value to consider a compound active."
    }
  ]
}
